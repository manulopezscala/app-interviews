import type { ReactNode } from 'react';
import { ArrowRight, Code2, Mic, Network, Video, Volume2, DoorOpen } from 'lucide-react';
import { Button } from '../components/ui/Button';

export default function InstruccionesDePreparacion() {
  return <div className="screen" style={{ display:'flex', flexDirection:'column' }}><header className="topbar" style={{ justifyContent:'center' }}><div className="logo">InterviewConnect</div></header><main style={{ flex:1, display:'grid', placeItems:'center', padding:48 }}><section className="card" style={{ width:960, padding:56, borderTop:'6px solid var(--brand)' }}><h1 className="page-title" style={{ textAlign:'center' }}>Guía de Preparación</h1><p className="page-subtitle" style={{ textAlign:'center', maxWidth:760, margin:'12px auto 40px' }}>Por favor, revisa la siguiente información antes de comenzar tu entrevista. Asegúrate de cumplir con todos los requisitos para garantizar una experiencia fluida.</p><div style={{ display:'grid', gridTemplateColumns:'1fr 1fr', gap:24 }}><Info icon={<Code2/>} title="Estructura" items={['Evaluación de Habilidades Técnicas (Live Coding).','Preguntas sobre Soft Skills y resolución de conflictos.','Espacio para tus preguntas al equipo.']}/><Info icon={<Volume2/>} title="Tiempo Estimado" items={['~ 45 Minutos','Te recomendamos reservar al menos 60 minutos en tu calendario para evitar interrupciones.']}/></div><section className="card" style={{ marginTop:24, padding:30 }}><h2 style={{ display:'flex', gap:12, alignItems:'center' }}><Network color="var(--brand)"/>Requisitos Técnicos</h2><div style={{ display:'grid', gridTemplateColumns:'1fr 1fr', gap:18 }}>{[[Network,'Conexión estable (Min 10Mbps)'],[Video,'Cámara web funcional'],[Mic,'Micrófono claro (Preferible auriculares)'],[DoorOpen,'Entorno silencioso']].map(([Icon,label]) => { const C = Icon as typeof Network; return <div key={label as string} style={{ display:'flex', gap:12, alignItems:'center', padding:18, background:'#FAFAFA', border:'1px solid var(--border)' }}><C color="var(--brand)"/>{label as string}</div>; })}</div></section><div style={{ display:'flex', justifyContent:'space-between', marginTop:36 }}><Button variant="ghost">Volver</Button><Button>Ir a prueba de equipo <ArrowRight/></Button></div></section></main><footer style={{ textAlign:'center', padding:24, color:'var(--muted)' }}>© 2024 InterviewConnect. All rights reserved.</footer></div>;
}
function Info({ icon, title, items }: { icon: ReactNode; title: string; items: string[] }) { return <section className="card" style={{ padding:30 }}><h2 style={{ display:'flex', gap:12, alignItems:'center' }}><span style={{ color:'var(--brand)' }}>{icon}</span>{title}</h2><ul style={{ color:'var(--muted)', lineHeight:1.8, paddingLeft:22 }}>{items.map(i=><li key={i}>{i}</li>)}</ul></section>; }
